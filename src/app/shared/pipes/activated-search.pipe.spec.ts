import { ActivatedSearchPipe } from './activated-search.pipe';

describe('ActivatedSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ActivatedSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
